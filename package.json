{
  "name": "siem-dashboard",
  "version": "1.0.0",
  "description": "Real-time SIEM Dashboard for GMU Hackathon",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "install:all": "npm install",
    "dev": "npx concurrently -n \"backend,frontend\" -c \"cyan,green\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start": "npm run start --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
